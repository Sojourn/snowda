#ifndef SNW_PROGRAM_H
#define SNW_PROGRAM_H

namespace Snowda {
    // class Program {
    // public:
    //     class Member {};

    //     Program();

    //     void freeze();

    //     TypeId registerType(ModuleId moduleId, StringView name);
    //     FunctionId registerFunction(TypeId argumentsTypeId, InstructionBuffer instructions);
    //     MemberId registerMember(StringView name, TypeId memberTypeId);

    //     const Type &type(TypeId typeId) const;
    //     const Type *type(StringView name) const;
    //     const Function &function(FunctionId functionId) const;
    //     const Member &member(MemberId memberId) const;

    // private:
    //     std::vector<Type> types_;
    //     std::vector<Function> functions_;
    //     std::map<StringView, TypeId> typeNames_;
    //     bool frozen_;
    // };
}

#endif // SNW_PROGRAM_H
