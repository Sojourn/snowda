#include "snw_code.h"

using namespace Snowda;

// Program::Program()
//     : frozen_(false)
// {
// }

// void Program::freeze() {
//     frozen_ = true;
// }

// TypeId Program::registerType(ModuleId moduleId, StringView name) {
//     if (types_.size() == std::numeric_limits<RawTypeId>::max()) {
//         return StringView("");
//     }
// }

// FunctionId Program::registerFunction(TypeId argumentsTypeId, InstructionBuffer instructions);
// MemberId Program::registerMember(StringView name, TypeId memberTypeId);

// const Type &type(TypeId typeId) const;
// const Type *type(StringView name) const;
// const Function &function(FunctionId functionId) const;
// const Member &member(MemberId memberId) const;
